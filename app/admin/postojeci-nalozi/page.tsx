"use client"; import {useState} from "react"; import {demoAccounts} from "@/lib/demo-data";
export default function ExistingAccounts(){const[msg,setMsg]=useState("");async function reset(email:string){const r=await fetch("/api/admin/password-reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,mode:"email"})});setMsg(r.ok?"Reset link je poslat.":"Podesite Supabase kljuceve za slanje reset linka.");}return <main><section className="page-title"><p className="eyebrow">Admin</p><h1>Postojeci nalozi</h1></section><section className="section"><div className="panel"><p>{msg}</p>{demoAccounts.map(a=><div className="admin-row" key={a.username}><span><b>{a.label}</b><small>{a.username} | {a.email}</small></span><button onClick={()=>reset(a.email)}>Posalji reset email</button></div>)}</div></section></main>}
